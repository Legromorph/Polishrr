# Run once per minute; scheduler_tick.py decides whether the configured cron
# expression is due and prevents overlapping runs.
* * * * * /usr/local/bin/python /app/scheduler_tick.py >> /app/runtime/cron.log 2>&1
